# Project execution notes

- 在把动态资源切换为自动生成产物前，必须先在目标平台完成一次真实生成，并验证所有被 README 引用的文件都存在；第三方 Action 可能吞掉局部错误并让整个任务显示成功，不能把绿色状态当成交付证据。
- 遇到第三方 Action 请求了与目标无关的额外权限时，应先移除不必要的查询或改用最小权限实现。不要为了绕过一个无关字段持续扩大 OAuth scope，更不能在没有明确确认交互方式时操作用户的可见浏览器。
- 迁移应保持旧展示链路可用，直到新产物验证成功后再切换引用；如果已经提前切换，必须优先恢复可用状态，再继续优化。
- 当目标只是读取稳定的官方字段并生成少量静态产物时，优先在项目内实现零依赖的小型生成器。不要围绕第三方 Action 的权限缺陷持续打补丁或扩权；外部实现已成为主要阻力时，应立即收缩依赖边界并直接写代码。
